class TaskModel{
 TaskModel({required this.title, required this.description});
  String? title;
  String? description;
  int? id;

}

//so i am tryint to create a class model that would ha ve a list of task and then
//save the file inside the list of class